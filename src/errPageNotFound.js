import React, { Component } from 'react'

export default class errPageNotFound extends Component {
    render() {
        return (
            <div>
                Page don't exist
            </div>
        )
    }
}
